import 'package:flutter/material.dart';

class ViewScreenWidget extends StatelessWidget {
  const ViewScreenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}